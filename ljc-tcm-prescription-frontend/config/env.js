// 环境配置
module.exports = {
    // 如果微信开发者工具和后端在同一台电脑，用 localhost
    BASE_URL: 'http://localhost:8081'

    // 如果需要真机调试或电脑不同，改成电脑的局域网IP：
    // BASE_URL: 'http://192.168.1.7:8081'  // 通过 ifconfig 查看你的实际IP

    // 生产环境改成：
    // BASE_URL: 'https://your-domain.com'
};
